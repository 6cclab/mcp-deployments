apiVersion: v1
kind: Service
metadata:
  name: homelab-mcp
  labels:
    app: homelab-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3100
  selector:
    app: homelab-mcp
